<style>
  body {
    background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
    min-height: 100vh;
  }

  .quiz-container {
    padding: 2rem 0;
    max-width: 100%;
  }

  .quiz-header {
    background: linear-gradient(145deg, #1f1f1f 0%, #2a2a2a 100%);
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
    margin-bottom: 2rem;
    border: 1px solid #ff6b35;
  }

  .quiz-header h1 {
    color: #ff6b35;
    font-weight: 700;
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    text-shadow: 0 2px 10px rgba(255, 107, 53, 0.3);
  }

  .welcome-text {
    color: #b0b0b0;
    font-size: 1.1rem;
  }

  .btn-create {
    background: linear-gradient(135deg, #ff6b35 0%, #ff8c42 100%);
    border: none;
    padding: 0.75rem 2rem;
    border-radius: 50px;
    font-weight: 600;
    color: white;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(255, 107, 53, 0.5);
    text-decoration: none;
    display: inline-block;
  }

  .btn-create:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(255, 107, 53, 0.7);
    color: white;
    background: linear-gradient(135deg, #ff8c42 0%, #ff6b35 100%);
  }

  .quiz-card {
    background: linear-gradient(145deg, #242424 0%, #1f1f1f 100%);
    border-radius: 20px;
    transition: all 0.3s ease;
    border: 1px solid #333;
    overflow: hidden;
    height: 100%;
    position: relative;
    box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
  }

  .quiz-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 20px 40px rgba(255, 107, 53, 0.3);
    cursor: pointer;
    border-color: #ff6b35;
  }

  .quiz-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 5px;
    background: linear-gradient(90deg, #ff6b35 0%, #ff8c42 100%);
    z-index: 1;
  }

  .quiz-card-body {
    padding: 2rem;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .quiz-title {
    color: #ffffff;
    font-weight: 700;
    font-size: 1.2rem;
    margin-bottom: 1rem;
    line-height: 1.4;
    word-wrap: break-word;
    overflow-wrap: break-word;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .quiz-description {
    color: #a0a0a0;
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    flex: 1;
    word-wrap: break-word;
    overflow-wrap: break-word;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .quiz-creator {
    display: flex;
    align-items: center;
    padding-top: 1rem;
    border-top: 2px solid #333;
    margin-top: auto;
  }

  .creator-avatar {
    width: 40px;
    height: 40px;
    min-width: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, #ff6b35 0%, #ff8c42 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 600;
    margin-right: 0.75rem;
    font-size: 0.9rem;
    box-shadow: 0 2px 10px rgba(255, 107, 53, 0.3);
  }

  .creator-info {
    flex: 1;
    min-width: 0;
  }

  .creator-label {
    font-size: 0.7rem;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 0.25rem;
    font-weight: 600;
  }

  .creator-email {
    color: #e0e0e0;
    font-weight: 600;
    font-size: 0.85rem;
    word-wrap: break-word;
    overflow-wrap: break-word;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .empty-state {
    text-align: center;
    padding: 4rem 2rem;
    background: linear-gradient(145deg, #242424 0%, #1f1f1f 100%);
    border-radius: 20px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
    border: 1px solid #333;
  }

  .empty-state-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    filter: grayscale(50%);
  }

  .empty-state h3 {
    color: #ffffff;
    margin-bottom: 0.5rem;
  }

  .empty-state p {
    color: #888;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .quiz-header h1 {
      font-size: 2rem;
    }
    
    .quiz-container {
      padding: 1rem 0;
    }

    .quiz-card-body {
      padding: 1.5rem;
    }

    .quiz-title {
      font-size: 1.25rem;
    }

    .quiz-description {
      font-size: 0.9rem;
    }

    .quiz-header {
      padding: 1.5rem;
      border-radius: 15px;
    }
  }

  @media (max-width: 576px) {
    .quiz-header h1 {
      font-size: 1.75rem;
    }

    .welcome-text {
      font-size: 1rem;
    }

    .quiz-card-body {
      padding: 1.25rem;
    }

    .quiz-title {
      font-size: 1.15rem;
    }

    .quiz-description {
      font-size: 0.85rem;
      line-height: 1.5;
    }

    .creator-avatar {
      width: 35px;
      height: 35px;
      min-width: 35px;
      font-size: 0.85rem;
    }

    .creator-email {
      font-size: 0.8rem;
    }

    .creator-label {
      font-size: 0.65rem;
    }

    .btn-create {
      width: 100%;
      padding: 0.65rem 1.5rem;
      text-align: center;
    }
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    .quiz-card-body {
      padding: 1.75rem;
    }

    .quiz-title {
      font-size: 1.3rem;
    }
  }

  @media (min-width: 1400px) {
    .quiz-title {
      font-size: 1.5rem;
    }
  }

  /* Equal height cards */
  .row-equal {
    display: flex;
    flex-wrap: wrap;
  }

  .card-col {
    display: flex;
    margin-bottom: 1.5rem;
  }
</style>

<div class="quiz-container">
  <div class="container-fluid px-4">
    
    <!-- Header -->
    <div class="quiz-header">
      <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center">
        <div>
          <h1 class="mb-1">Quizzes</h1>
          <p class="welcome-text mb-0">Welcome, <%= current_user.email %> 👋</p>
        </div>

        <% if current_user&.admin? %>
          <%= link_to "Create New Quiz", new_quiz_path, class: "btn btn-create mt-3 mt-md-0" %>
        <% end %>
      </div>
    </div>

    <!-- Quiz Cards -->
    <% if @quizes.any? %>
      <div class="row g-4">
        <% @quizes.each do |quiz| %>
          <div class="col-12 col-sm-6 col-md-4 col-lg-3 card-col">
            <div class="card quiz-card w-100">
              <div class="card-body quiz-card-body">
                <!-- Title -->
                <h5 class="quiz-title"><%= quiz.title %></h5>

                <!-- Description -->
                <p class="quiz-description">
                  <%= truncate(quiz.description, length: 120) %>
                </p>

                <!-- Creator info -->
                <div class="quiz-creator">
                  <div class="creator-avatar">
                    <%= quiz.user.email.first.upcase %>
                  </div>
                  <div class="creator-info">
                    <div class="creator-label">Created by</div>
                    <div class="creator-email" title="<%= quiz.user.email %>">
                      <%= quiz.user.email %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <div class="empty-state-icon">📝</div>
        <h3>No quizzes yet</h3>
        <p>Create your first quiz to get started!</p>
      </div>
    <% end %>

  </div>
</div>